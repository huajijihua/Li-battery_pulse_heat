# 模型说明 V4-F

日期：2026-07-14

V4 工作副本：`02_模型/pulse_heating_official_spine_v04.slx`
v03 诊断沙箱：已归档至 `00_任务计划与资料/归档/v03_诊断沙箱/`（2026-07-14），不再进入 active 路径
V4 建模主计划：`00_任务计划与资料/V4_建模主计划_v01.md`

## 1. 项目定位

本项目是**通用车载脉冲加热系统仿真平台**，不是具体产品仿真或特定车型仿真。模型服务于结构规范、能量路径、控制链、限制接口、KPI 语义和验证闭环。

默认参数来源：`OFFICIAL`（MathWorks 官方）、`PLATFORM_DEFAULT`（平台自洽）、`ENGINEERING_DEFAULT`（工程经验）、`PLACEHOLDER`（明确缺口）。公司历史参数和 L0.5 粗筛参数不进入默认模型。详见 `00_任务计划与资料/剥离原则与参数分层规则_v01.md`。

## 2. 当前状态

`pulse_heating_official_spine_v04.slx` 是 V4-F 完成后的工作副本。顶层 6 大模块边界已建立，物理 DC-link 已闭合（V4-D3），4 个损耗功率已从 NaN 替换为真实计算（V4-E），DC 量 KPI 和电压裕度已填充（V4-G），电池电化学限制和析锂风险标志已接入（V4-F）。

当前模型状态摘要：

| 域 | 完成阶段 | 关键状态 |
|---|---|---|
| 电驱边界 | V4-B2/D2.1/D2.2 | 官方逆变器+PMSM+热模型，DC 母线拓扑已修复，PI 增益已重计算，PWM 载波已接入 |
| 控制器边界 | V4-B3 | 官方 MCB FOC（Closed Loop Control + SVPWM），Idq_debug 已引出 |
| 电池边界 | V4-B4/D3/F | 官方 Battery_pack + Coolant_control，物理 DC-link 已闭合，低温初始条件 -10°C，T_min/SOC_limit/Charge_limit/Discharge_limit 已接入 |
| DC_Link 边界 | V4-D3/G | 物理 PMIOPort 闭合，DC_Current_Sensor + DC_Voltage_Sensor 已接入，V_dc 已校准为 70V |
| 命令边界 | V4-B6 | 堵转脉冲 Idq_ref，LimitConfig 输出 |
| 损耗计算 | V4-E | P_cu/P_iron/P_inv/P_batt_heat 均有数值，Energy_Balance_Audit 已建立 |
| 限制接口 | V4-C/G/F | LimitStatus 10 维，V_margin/safety_margin 已填充，剩余 3 个 NaN（batt_temp_margin/coolant_flow_margin/track_err 待 V4-H） |
| KPI 接口 | V4-D1/D2/E/G | SystemKPI 24 维，19+ 维有数值，剩余 NaN 属于 V4-H/I 范围 |
| Simscape logging | V4-E | SimscapeLogType=all，18 个 power_dissipated 变量可用 |
| 低温工况 | V4-F | ambient=263.15K(-10°C)，cellT_ini 已覆盖 |

**V4-F 仍不是行为模型**：能量守恒残差大（77.7%，含未计入的开关损耗/电缆损耗/无功功率）；tracking_error 未计算；电流限幅未接入；长仿真温升曲线未验证。后续 V4-H（控制策略）和 V4-I（行为验证）解决这些问题。

**V4-D2 仍不是行为模型**：限制接口字段顺序和 UNKNOWN 语义已稳定，部分控制/电流字段已有数值，但电池端、DC-link 和安全裕度仍缺失，不能用于通过/不通过判定。

## 3. V4-D2 KPI 接口

V4-D1 新增 `SystemKPI` 24 维接口；V4-D2 保持字段顺序不变，只替换部分字段来源：

```text
SystemKPI = [
  I_battery_terminal_rms_A;
  I_dc_link_rms_A;
  I_motor_phase_rms_A;
  I_heat_equiv_rms_A;
  V_battery_terminal_V;
  V_dc_link_V;
  modulation_index;
  voltage_margin;
  P_battery_heat_W;
  P_motor_copper_W;
  P_motor_iron_W;
  P_inverter_loss_W;
  T_battery_C;
  T_motor_stator_C;
  T_motor_rotor_C;
  tracking_error_rms_A;
  control_saturation_flag;
  closed_loop_enable;
  current_limit_margin;
  thermal_margin;
  battery_charge_limit_margin;
  unknown_safety_margin;
  limiting_factor_code;
  kpi_status_code
]
```

当前字段来源：
- `I_motor_phase_rms_A = DriveKPI[1] = sqrt((ia^2 + ib^2 + ic^2)/3)`，来自 `From_i_abc -> Compute_Current_KPI_V4D2`
- `V_dc_link_V = DCBus`
- `modulation_index = ControlKPI[2] = max(abs(2*duty_abc - 1))`，来自 `Control_System/Duty_abc -> Compute_Duty_KPI_V4D2`
- `voltage_margin = limit_margin_unknown_value`
- `P_motor_copper_W = DriveKPI[3]`
- `P_motor_iron_W = DriveKPI[4]`
- `P_inverter_loss_W = DriveKPI[5]`
- `T_battery_C = BatteryStatus[1]`
- `T_motor_stator_C = DriveKPI[6]`
- `T_motor_rotor_C = DriveKPI[7]`
- `tracking_error_rms_A = ControlKPI[1]`
- `control_saturation_flag = ControlKPI[3] = any(duty_abc <= 0 | duty_abc >= 1)`
- `closed_loop_enable = ControlKPI[4]`
- `battery_charge_limit_margin = BatteryStatus[6]`
- `unknown_safety_margin = limit_margin_unknown_value`
- `limiting_factor_code = limit_factor_unknown_code = 99`
- `kpi_status_code = kpi_status_v4d2_partial_numeric_code = 6`
- 其余电池端电流、DC-link RMS、电池热功率、热等效电流、电池端电压、电流限制和热裕度字段当前为 `kpi_value_unknown = NaN`

`SystemKPI` 顶层输出当前接到 `Terminate_SystemKPI`。`03_脚本/run_v4d_minimal_regression.m` 检查接口契约、模型工作区参数和 0.001 s 烟测；`03_脚本/run_v4d2_case_matrix_regression.m` 通过临时 `To Workspace` 块读取 `SystemKPI/LimitStatus`，脚本结束后删除临时块，不导出文件。

## 4. V4-D2.2 验证结果

| 测试 | 结果 | 判断 |
|---|---|---|
| `model_check(all)` | `healthy` | 结构连通性通过 |
| 逆变器 DC 母线拓扑读回 | DC+ 连接 A(H).C/B(H).C/C(H).C；DC- 连接 A(L).E/B(L).E/C(L).E | 与官方拓扑一致 |
| PI 增益读回 | Kp_i=2.347, Ki_i=153.4 (was 2/3643) | 设备适配修复通过 |
| PWM 载波读回 | Repeating Sequence, fsw=2000Hz, 0->1 锯齿波 | 替换阈值比较通过 |
| pmsm 元数据读回 | J=0.2, Ke=0.18, Kt=0.27 (was 7.06e-6/4.64/0.0384) | 元数据覆盖通过 |
| 关闭重载后最小仿真 | `StopTime=0.001`，`ReachedStopTime`，`tout(end)=0.001` | 烟测通过；不是行为验证 |
| 模型工作区参数读回 | 7 个阶段 4 参数读回一致 | 参数持久化通过 |
| 电池边界读回 | `Battery_pack` + `Coolant_control` + `Controlled_Current_Source` + `BatteryLoad_NaN_to_Zero` | 已恢复为官方电池边界骨架 |
| 限制接口读回 | `KPI_And_Logging/LimitStatus` 10 维输出 | 字段顺序稳定；UNKNOWN 主导 |
| V4-C 参数读回 | `limit_factor_unknown_code=99`，`limit_status_v4c_code=5` | 参数持久化通过 |
| KPI 接口读回 | `KPI_And_Logging/SystemKPI` 24 维输出 | 字段顺序稳定；UNKNOWN 主导 |
| V4-D2 参数读回 | `kpi_value_unknown=NaN`，`kpi_status_v4d2_partial_numeric_code=6`，`drive_status_v4d2_partial_current_code=6`，`control_status_v4d2_partial_duty_code=6` | 参数持久化通过 |
| 诊断仿真（50Hz/1A/0.01s） | `i_a max=7.7A`，`Id_fb max=7.7A`（was 15.4A） | PWM 载波修复后振荡减半 |
| `run_v4d_minimal_regression.m` | `passed=true`，`ReachedStopTime`，`finalTime_s=0.001` | 最小接口回归通过；不是行为验证 |
| `run_v4d2_case_matrix_regression.m` | 4 个 case 全部 `passed=1` | 多工况烟测通过；不是工程可行性证明 |

V4-D2.2 多工况结果摘要（设备适配修复后）：
- `zero_cmd`: `IphaseRMS=0.000665`，`mod=0.00379`，`sat=0`，`kpiStatus=6`，`limitStatus=5`（was 5.783，振荡降低 4 个数量级）
- `low_freq_small_current`: `IphaseRMS=0.000665`，`mod=0.0770`，`sat=0`，`kpiStatus=6`，`limitStatus=5`（was 4.294）
- `diagnostic_small_current`: `IphaseRMS=5.465`，`mod=0.508`，`sat=0`，`kpiStatus=6`，`limitStatus=5`（高频脉冲瞬态，非稳态振荡）
- `missing_boundary_guard`: `IphaseRMS=5.465`，`mod=0.514`，`sat=0`，`kpiStatus=6`，`limitStatus=5`

说明：V4-D2.2 修复了 PI 增益、PWM 载波和元数据三个设备适配失配。零命令振荡从 5.78A 降至 0.00067A，验证了修复有效性。诊断频率工况的 5.47A 是高频脉冲在短仿真时间内的瞬态响应。

## 5. V4-B6 命令路径

```text
Id_Pulse_Stall(0/1) -> 2*pulse_id_amplitude_A -> -pulse_id_amplitude_A -> Id_ref
Iq_ref_zero_A -> Iq_ref
[Id_ref; Iq_ref] -> MCB_SIUnits_FOC_Controller/Idq_ref

LimitConfig = [Id_amp_A; diagnostic_frequency_Hz; duty_percent; phase_delay_s; mode_code; status_code]
```

机械端仍沿用 V4-B2 的堵转边界：PMSM 机械端经 Encoder 与 Motor inertia/Mechanical Rotational Reference 连接，`Speed_ref=0`。

## 6. 当前有效文件

| 路径 | 角色 | 状态 |
|---|---|---|
| `02_模型/pulse_heating_official_spine_v04.slx` | V4-F 工作副本 | 物理 DC-link 闭合，4 损耗已接入，电池电化学限制已接入，低温 -10°C，SimscapeLogType=all |
| `03_脚本/build_pulse_heating_official_spine_v04a.m` | V4-A 构建 | 保留 |
| `03_脚本/build_pulse_heating_official_spine_v04b1.m` | V4-B1 构建（已取代） | 保留为历史参考 |
| `03_脚本/build_pulse_heating_official_spine_v04b2.m` | V4-B2 构建（含 DC 母线拓扑修复） | 保留 |
| `03_脚本/build_pulse_heating_official_spine_v04b3.m` | V4-B3 构建（含 PI 重计算和元数据覆盖） | 保留 |
| `03_脚本/build_pulse_heating_dc_link_v04d3.m` | V4-D3 信号级 DC-link 传感器（实验脚本） | 保留 |
| `03_脚本/close_physical_dc_link_v04d3.m` | V4-D3 物理 DC-link 闭合 | 保留 |
| `03_脚本/fix_inverter_dc_bus_topology_v04d2.m` | V4-D2.1 逆变器 DC 母线拓扑修复 | 保留 |
| `03_脚本/restore_pulse_heating_v04d2_2.m` | V4-D2.2 可重放恢复脚本 | 保留 |
| `03_脚本/run_v4d_minimal_regression.m` | V4-D 最小接口回归 | 保留 |
| `03_脚本/run_v4d2_case_matrix_regression.m` | V4-D2 多工况回归 | 保留 |
| `00_任务计划与资料/V4_建模主计划_v01.md` | V4 建模主计划 | active |
| `00_任务计划与资料/剥离原则与参数分层规则_v01.md` | 参数准则 | active |
| `00_任务计划与资料/官方案例复用资产清单与重构路线_v01.md` | 复用清单 | active |
| `00_任务计划与资料/归档/v03_诊断沙箱/` | v03 模型+脚本（2026-07-14 归档） | 不再 active |

V4-E/F/G 阶段的模型改动通过 MCP `model_edit` 和 MATLAB API 直接操作 `.slx` 完成，未生成独立构建脚本。新增 MATLAB Function 块的代码内嵌在模型中。

## 7. v03 诊断参考（已归档，仅供 Git 历史对照）

v03-B3 诊断结论：`model_check=all healthy`；`0 A` 工况正常；`5/20/100/200 A` 闭环工况未触及调制饱和但高频跟踪误差主导。v03 只做到库块级复用。旧 `battery_pack_252S1P` 资产已删除（Git `e23e0dc`）。v03 模型和脚本已于 2026-07-14 归档至 `00_任务计划与资料/归档/v03_诊断沙箱/`，不再进入 active 路径。

## 8. V4-D3 DC-link 闭合状态（2026-07-14 完成）

### 8.1 物理闭合

- 删除电驱边界临时 `Battery_DC_Source`（48V batteryecm_lib/Battery）
- 删除电池边界 CCS/Solver_Configuration/Electrical_Reference/BatteryLoad_NaN_to_Zero/BatteryLoad_to_PS
- 两侧新增 PMIOPort（`Battery_pos`/`Battery_neg`、`DC_plus`/`DC_minus`），根级物理连接闭合
- 新增 `DC_Current_Sensor`（串联）和 `DC_Voltage_Sensor`（并联），经 PS-Simulink Converter -> Goto 广播
- `I_dc_UNKNOWN`/`P_dc_UNKNOWN`/`V_pack_UNKNOWN`/`I_pack_UNKNOWN` NaN 常量替换为传感器实测信号
- `inverter.V_dc` 从 48 改为 66（电池包 OCV ~70V）

### 8.2 Goto/From tag scope 修复（2026-07-14）

`V_pack_goto` 和 `I_dc_goto` 的 `TagVisibility` 原为 `local`，导致跨子系统 From 块（电池边界的 `V_pack_from_drive`/`I_pack_from_drive`）读不到信号，静默返回 0。已改为 `global`。

此问题曾导致误判"V_pack=0V，DC-link 闭合失败"，实际物理层电压正确（70.3V），但信号层 Goto/From 链路断裂。

### 8.3 验证结果

| 测试 | 结果 |
|---|---|
| `model_check(all)` | healthy（1 个 dangling line warning，非 error） |
| 0.001s 烟测 | ReachedStopTime |
| `run_v4d_minimal_regression.m` | passed |
| `run_v4d2_case_matrix_regression.m` | 4/4 passed |
| Simscape log V_pack | 70.3V（SOC=0.8, 4 模块串联） |
| Simscape log I_dc peak | 11.2A（1A/50Hz 脉冲） |
| Root-level From 验证 | 70.3V（tag scope 修复后） |

### 8.4 Simscape 传感器端口布局

在 R2025b 中，`fl_lib` 的 Current Sensor 和 Voltage Sensor 的物理端口布局为：

| 块 | LConn1 | RConn1 | RConn2 |
|---|---|---|---|
| Current Sensor | p (+电气) | I (物理信号输出) | n (-电气) |
| Voltage Sensor | p (+电气) | V (物理信号输出) | n (-电气) |

**RConn1 是信号端口，不是电气端口。** 连接电气 n 端时必须用 RConn2，否则会报"物理建模域连接规则"错误。

## 9. 信号验证规范（防止 Goto/From scope 类问题）

### 9.1 跨子系统 Goto/From 规则

- 新增 Goto/From 跨子系统信号时，**必须**检查 `TagVisibility`：
  - `local`（默认）：仅同一子系统内可见
  - `global`：全模型可见
- 如果 Goto 和 From 不在同一个子系统内，**必须**设为 `global`
- 每次 `model_edit` 或脚本新增 Goto 块后，用以下命令审计：
  ```matlab
  gotoBlocks = find_system(modelName, 'BlockType', 'Goto');
  for i = 1:numel(gotoBlocks)
      tag = get_param(gotoBlocks{i}, 'GotoTag');
      scope = get_param(gotoBlocks{i}, 'TagVisibility');
      fprintf('  %s: tag="%s" scope=%s\n', gotoBlocks{i}, tag, scope);
  end
  ```

### 9.2 信号验证双重确认

读取仿真结果时，**不能只依赖一种捕获方式**。以下两种方式之一为正时仍需交叉验证：

| 方式 | 优点 | 缺点 |
|---|---|---|
| Simscape log (`simlog.XXX.Y.v.series.values`) | 直接读取物理量，不经过信号链路 | 需要 `SimscapeLogType=all` |
| To Workspace / Outport | 读取 Simulink 信号链路末端 | 受 Goto/From scope、PS-Simulink Converter 连接等影响 |

**规则：当 Simscape log 和 To Workspace 结果不一致时，以 Simscape log 为准，并排查信号链路断点。**

### 9.3 零值检查

仿真后如果任何 KPI 字段或传感器读数为 0，**必须**用 Simscape log 交叉验证，不能直接假设"物理上确实是 0"。0 值的常见原因：

1. Goto/From tag scope 不匹配（local vs global）
2. PS-Simulink Converter 连接到错误的传感器端口（如 RConn1 信号端口而非 RConn2 电气端口）
3. From 块找不到匹配的 Goto 时，Simulink 静默返回 0，不报错
4. To Workspace `SaveFormat=Array` 在单步仿真时只保存最终值

## 10. V4-E 损耗与发热计算（2026-07-14 完成）

### 10.1 新增块

| 块名 | 类型 | 功能 | 来源标签 |
|---|---|---|---|
| `Compute_P_cu_V4E` | MATLAB Function | `P_cu = 1.5*Rs*(Id_fb²+Iq_fb²)`，输入 Idq_debug[4] | OFFICIAL 公式 |
| `Compute_P_iron_V4E` | MATLAB Function | `P_iron = abs(trq_loss * w_motor)`，复用官方 losses 函数 | OFFICIAL 结构 |
| `Compute_P_inv_V4E` | MATLAB Function | `P_inv = 2*(Vf*I_avg + Ron*I_rms²)`（导通损耗，不含开关损耗） | ENGINEERING_DEFAULT |
| `Compute_P_batt_heat_V4E` | MATLAB Function | `P_batt_heat = I_pack² * R_pack`，R_pack=0.1785Ω | ENGINEERING_DEFAULT |
| `Energy_Balance_Audit` | MATLAB Function | 残差 = P_batt_terminal - (P_inv+P_cu+P_iron+P_batt_heat+P_mech) | ENGINEERING_DEFAULT |

### 10.2 信号路由

- `Idq_debug`：从 Control_System 内部引出（Goto tag `Idq_debug_V4E`，global），经 From -> Compute_P_cu_V4E
- `trq_loss`：从 Scopes/losses 子系统引出（Goto tag `trq_loss_V4E`，global），经 From -> Compute_P_iron_V4E
- `w_motor`：复用已有 global Goto tag `wMotor`
- `i_abc`：复用已有 global Goto tag `i`
- `I_pack`：复用 V4-D3 的 global Goto tag `I_dc_meas`
- `V_pack`：复用 V4-D3 的 global Goto tag `V_pack_meas`

### 10.3 替换的 NaN 常量

| 旧块（NaN） | 新块 | DriveKPI/KPI 位置 |
|---|---|---|
| `P_cu_UNKNOWN` (blk_193) | `Compute_P_cu_V4E` -> `P_cu_V4E` | DriveKPI[3] |
| `P_iron_UNKNOWN` (blk_194) | `Compute_P_iron_V4E` -> `P_iron_V4E` | DriveKPI[4] |
| `P_inv_UNKNOWN` (blk_195) | `Compute_P_inv_V4E` -> `P_inv_V4E` | DriveKPI[5] |
| `KPI_P_batt_heat_UNKNOWN` (blk_570) | `Compute_P_batt_heat_V4E` -> `P_batt_heat_V4E` | SystemKPI[9] |

### 10.4 Simscape logging

- `SimscapeLogType` 已设为 `all`，`SimscapeLogName` = `simlog_v4e`
- simlog 中可用的 `power_dissipated` 变量（共 18 个）：
  - PMSM：1 个（`PMSM.power_dissipated`）
  - IGBT：12 个（6 `ideal_switch` + 6 `diode`）
  - 电池电缆：5 个（R12/R23/R34/neg_cable/pos_cable）
- BatteryModule 内部无 `power_dissipated`，电池电芯发热用外部公式计算

### 10.5 验证结果（50Hz/1A/0.02s 诊断仿真）

| 损耗项 | 公式值（mean） | simlog 精确值（mean） | 说明 |
|---|---|---|---|
| P_cu | 0.76 W | N/A（PMSM.power_dissipated=0.0005W，不区分铜损/铁损） | 公式从 Idq 计算 |
| P_iron | 0.00 W | N/A | 堵转时 w=0，铁损为 0（物理正确） |
| P_inv | 2.47 W | 3.99 W | 公式只含导通损耗；simlog 含开关损耗 |
| P_batt_heat | 2.22 W | N/A（BatteryModule 无 power_dissipated） | I_pack²×R_pack |
| P_batt_terminal | 22.29 W | N/A | V_pack × I_pack |
| P_loss_total | 4.97 W | ~7.0 W（公式+simlog 差值估算） | 4 项损耗之和 |
| 残差 | 17.32 W (77.7%) | N/A | 含未计入的开关损耗、电缆损耗、RC 网络损耗和无功功率 |

### 10.6 已知限制

1. **P_inv 只含导通损耗**：信号链公式 `2*(Vf*I_avg + Ron*I_rms²)` 不含开关损耗；simlog 精确值（含开关损耗）可通过后处理脚本提取
2. **P_batt_heat 只含电芯内阻发热**：不含电缆连接损耗（R12/R23/R34/neg_cable/pos_cable，simlog max=0.62W）和 RC 网络损耗
3. **能量守恒残差大**：主要因为脉冲电流的瞬时功率波动大、公式估算不完整、无功功率交换；后续 V4-G/V4-I 通过 simlog 后处理做精确审计
4. **P_iron 在堵转时为 0**：这是物理正确行为（w=0 时无铁损），不是计算错误

## 11. V4-G DC-link 与能量路径完善（2026-07-14 完成）

### 11.1 inverter.V_dc 校准

- InitFcn 中 `inverter.V_dc` 从 66 改为 70（基于仿真实测 V_pack mean=70.15V，ENGINEERING_DEFAULT）
- SVPWM Normalize gain 从 0.01312 变为 0.01237（-5.72%），调制指数更准确
- 官方 PMSMDriveThermal 无 DC-link 电容，电池内阻和 RC 网络已提供滤波，当前 V_pack sag=1.54V（2.2%）可接受

### 11.2 DC 量 KPI 字段填充

| SystemKPI 字段 | 旧值 | 新值 | 来源 |
|---|---|---|---|
| [1] I_battery_terminal_rms | NaN | `abs(I_pack)` | Compute_I_rms_V4G |
| [2] I_dc_link_rms | NaN | `abs(I_pack)` | Compute_I_rms_V4G |
| [4] I_heat_equiv | NaN | `abs(I_pack)` | Compute_I_rms_V4G |
| [5] V_battery_terminal | NaN | `V_pack` | From V_pack_meas |

### 11.3 电压裕度填充

| 接口字段 | 旧值 | 新值 | 公式 |
|---|---|---|---|
| SystemKPI[8] V_margin | NaN | `V_dc*(1-mod_idx)` | Compute_Voltage_Margin_V4G |
| LimitStatus[1] V_margin | NaN | `V_dc*(1-mod_idx)` | 同上 |

### 11.4 能量方向审计增强

Energy_Balance_Audit 新增 2 个输出：`P_charge`（I_pack<0 时回充功率）和 `P_discharge`（I_pack>=0 时放电功率），接 Terminator 保留。

### 11.5 验证结果（50Hz/1A/0.02s）

| 指标 | V4-E | V4-G |
|---|---|---|
| SystemKPI numeric | 16/24 | **19/24** |
| LimitStatus V_margin | NaN | **65.65V** |
| V_batt | NaN | **70.31V** |
| I_batt_rms | NaN | **0.0042A** |
| I_dc_rms | NaN | **0.0042A** |
| I_heat_equiv | NaN | **0.0042A** |
| model_check | 1 warning | 1 warning（同一 dangling line） |

剩余 5 个 NaN 属于 V4-F（batt_charge_margin, batt_temp_margin, coolant_flow_margin）和 V4-H（track_err, I_limit_margin, thermal_margin）范围。

## 12. V4-F 电池电化学限制（2026-07-14 完成）

### 12.1 新增块

| 块名 | 类型 | 功能 | 来源标签 |
|---|---|---|---|
| `Compute_T_min_V4F` | MATLAB Function | `T_min = min(Pack_T)`，从 4 模块温度取最小值 | OFFICIAL 结构 |
| `Sel_SOC_min_V4F` | MATLAB Function | 从 SOC_limits [min;max] 提取 SOC_min | OFFICIAL 结构 |
| `Compute_Battery_Limits_V4F` | MATLAB Function | Charge_limit / Discharge_limit 通用公式 | LITERATURE + ENGINEERING_DEFAULT |
| `Compute_Lithium_Risk_V4F` | MATLAB Function | 析锂风险标志 + 安全裕度 | PLACEHOLDER |

### 12.2 替换的 NaN 常量

| 旧块（NaN） | 新块 | BatteryStatus 位置 |
|---|---|---|
| `T_min_UNKNOWN` (blk_519) | `Compute_T_min_V4F` -> `T_min_V4F` | BatteryStatus[2] |
| `SOC_limit_UNKNOWN` (blk_520) | `Sel_SOC_min_V4F` <- `SOC_limits_V4F` | BatteryStatus[3] |
| `Charge_limit_UNKNOWN` (blk_521) | `Compute_Battery_Limits_V4F` -> `charge_limit_V4F` | BatteryStatus[6] |
| `Discharge_limit_UNKNOWN` (blk_522) | `Compute_Battery_Limits_V4F` -> `discharge_limit_V4F` | BatteryStatus[7] |
| `Limit_unknown_safety_UNKNOWN` | `Compute_Lithium_Risk_V4F` -> `safety_margin_V4F` | LimitStatus[8] + SystemKPI[22] |

### 12.3 Charge_limit / Discharge_limit 公式

```text
Charge_limit = I_base * f(T) * g(SOC)
  I_base = cellAH * 1C = 27A
  f(T): T >= 273.15K (0C) -> 1.0; T < 273.15K -> max(0.1, (T-253.15)/20)
  g(SOC): SOC < 0.8 -> 1.0; SOC >= 0.8 -> max(0.1, (1-SOC)/0.2)

Discharge_limit = I_base * h(T)
  h(T): T < 263.15K (-10C) -> 0.5; T >= 263.15K -> 1.0
```

来源：LITERATURE（文献趋势）+ ENGINEERING_DEFAULT（通用公式参数）
文献依据：
- 析锂判据：负极固液相电位差 < 0 V vs Li/Li+（4 篇文献一致）
- 低温充电安全温度阈值：0°C（文献 1/3/4）
- 安全电流限制：文献 1 Table A1（50% SOC, 1000Hz, -20~5°C 范围）

### 12.4 低温初始条件

- InitFcn 中 `BatteryThermalManagementInitialization` 之后覆盖：`ambient=263.15` K（-10°C）、`coolantTemp=263.15` K、`cellT_ini=ambient*ones(...)`、`cellT_ini4=ambient*ones(...)`
- 所有 BatteryModule 的初始温度设为 -10°C

### 12.5 析锂风险标志（PLACEHOLDER）

```text
risk_flag:
  0 = Safe (T >= 0C or I_pack < 0.8*charge_limit)
  1 = Warning (T < 0C and safety_margin < 20% of charge_limit)
  2 = Risk (T < 0C and I_pack > charge_limit)

safety_margin = charge_limit - abs(I_pack)
```

注意：当前为简化代理标志，真正的析锂检测需要 P2D 电化学模型或 EIS 数据。

### 12.6 验证结果（-10°C, 50Hz/1A/0.02s）

| 指标 | V4-G | V4-F |
|---|---|---|
| 初始温度 | 25°C (298.15K) | **-10°C (263.15K)** |
| T_min | NaN | **263.15K (-10°C)** |
| SOC_limit | NaN | **从 SOC_limits 子系统接入** |
| Charge_limit | NaN | **13.5A** (27A * 0.5 * 1.0) |
| Discharge_limit | NaN | **13.5A** (27A * 0.5) |
| safety_margin | NaN | **2.98A** (13.5 - 10.52) |
| V_pack mean | 70.15V | 68.64V（低温略低） |
| I_dc max | 11.16A | 10.52A |
| 析锂风险 | N/A | **0 (SAFE)**（I_pack < charge_limit） |

## 13. 下一步

V4-F 已完成 4 个电池电化学限制字段脱离 NaN，并建立析锂风险标志。后续按 V4-H -> V4-I 推进：

1. **V4-H 控制策略完善**：tracking_error、电流限幅、脉冲指令与限制联动
2. **V4-I KPI 补全与行为验证**：SystemKPI 24 维全有数值、长仿真温升曲线、能量守恒闭环验证

每完成一个子阶段，同步更新本文件和 `V4_建模主计划_v01.md`，并提交 git。
