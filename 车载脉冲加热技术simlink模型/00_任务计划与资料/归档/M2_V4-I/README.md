# M2 V4-I 冻结证据包

本目录保存 `pulse_heating_official_spine_v04.slx` 的 M2/V4-I 构建记录、模型说明、参数冲突、能力审计和结果证据。`02_模型/`、`03_脚本/` 和 `outputs/` 保持原相对结构，仅用于受控追溯或只读复现。

目录内文件只用于追溯当时的结构、接口、诊断结果、已知问题和风险。其“当前”“下一步”“必须”等措辞只反映原记录日期，不构成 M3 的现行执行指令。

M2 模型和相关脚本保持冻结，不得据此继续扩展或修复。M3 的唯一 active 规划入口为 `00_项目管理/M3_工程兼容性模型规划_v01.md`。
