# M4-01 结构恢复与能量账本准备 v01

日期：2026-07-16
阶段：M4-01，活动 V4 最小内部改造首轮
状态：结构恢复完成，账本与参数身份准备中
活动基线：`车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx`

## 1. 本轮目标与边界

本轮目标是恢复并审计低温脉冲加热系统首轮主路径：

```text
电池物理端 -> HV 路径/DC-link -> 三相逆变器 -> PMSM -> 机械边界与热网络
```

本轮只做活动 V4 内的最小结构恢复和接口读回，不改变六模块顶层边界，不运行工程工况，不形成设备、安全、析锂、寿命或量产结论。

## 2. 已完成事实

### 2.1 结构恢复

| 范围 | 处理结果 | 证据 |
|---|---|---|
| `blk_20 DC_Link_And_Inverter_Interface` | 恢复 HV 正/负路径电阻和 DC-link 电容；正负路径分别从 HV 端经过路径电阻到逆变器端，电容并联在逆变器正负端 | `model_read(scope=blk_20, depth=0/1)` |
| root 负极边界 | 修复曾被误清除/旁路的连接，当前为 `Battery_neg -> HV_neg -> HV_Neg_Path_R -> Inverter_neg -> DC_minus` | root `model_read` 与 MATLAB `PortConnectivity` 读回 |
| `blk_66 Three-phase inverter` | 六个 IGBT、Phase Splitter、门极和正负母线恢复；内部 scope 检查通过 | `model_read/check(scope=blk_66)` |
| `blk_86 Thermal model` | 恢复转子、绕组 A/B/C、环境、绕组间导热/对流和温度测量节点 | `model_read/check(scope=blk_86)` |
| `blk_128 Encoder` | 恢复转矩传感器、转速/角度传感器、惯量和机械参考的连接 | `model_read/check(scope=blk_128)` |
| root/关键模块 | root、`blk_14`、`blk_20`、`blk_27`、`blk_35`、`blk_66`、`blk_86`、`blk_128` 均为 `healthy` | `model_check(all)` |

### 2.2 视觉复核

已对自动重排后的 root、`blk_20`、`blk_27`、`blk_66`、`blk_86`、`blk_128` 画布做视觉审查，重点检查断线、悬空线、端口错接、节点短接和明显重叠。

视觉证据位于系统临时目录，仅用于本轮审查，不进入项目 Active 资产：

```text
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_root.png
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_sid_20.png
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_sid_27.png
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_sid_66.png
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_sid_86.png
C:\Users\ADMIN\AppData\Local\Temp\opencode\pulse_heating_v4_sid_128.png
```

视觉检查支持以下结构判断，但不替代 `model_read`、`model_check` 或参数审计：

- `blk_20` 的两个路径电阻和 DC-link 电容确实落在物理蓝线网络中。
- `blk_27` 的 DC 电流传感器位于逆变器正端支路，DC 电压传感器跨逆变器 DC 正负端。
- `blk_66` 的六管桥、三相分相器和正负母线没有明显画布断线。
- `blk_86` 的转子、三相绕组、环境和温度测点连接成热网络。
- `blk_128` 的机械传感器、转矩路径、惯量和机械参考形成闭合机械边界。

## 3. 参数与结论边界

当前结构健康不等于配置可运行。已读回的关键状态包括：

| 对象 | 当前值/状态 | 身份与限制 |
|---|---|---|
| `blk_740 HV_Pos_Path_R` | `HVPath_R_PLACEHOLDER` | 高压路径电阻未知，不能形成损耗或热源结论 |
| `blk_741 HV_Neg_Path_R` | `HVPath_R_PLACEHOLDER` | 高压路径电阻未知，不能形成损耗或热源结论 |
| `blk_742 DC_Link_Capacitor` | `DCLink_C_PLACEHOLDER`，初始 `vc=0`，未指定初始目标 | DC-link 储能和瞬态响应仍阻断 |
| 电池低温阻抗/允许脉冲电流 | 未接入目标设备数据 | `PLACEHOLDER`/数据缺口 |
| 逆变器与 PMSM 能力边界 | 结构来自官方候选，部分参数为模型变量或示例身份 | `OFFICIAL` 结构证据，不是目标设备参数 |
| RMS | `blk_685` 仍为 `abs(I_instant)` 近似 | 不能作为时间窗口 RMS |
| 仿真 | 未运行有效工程工况 | 占位参数和契约缺口仍未解除 |

## 4. 当前接口语义审计

当前 `blk_27` 已有逆变器侧 DC 测量候选：

| 量 | 当前测点 | 当前解释 |
|---|---|---|
| `I_dc` | `blk_591 DC_Current_Sensor`，位于 `DC_plus` 到逆变器正端内部支路 | 逆变器侧 DC 电流候选，不等同于电池支路电流 |
| `V_dc` | `blk_592 DC_Voltage_Sensor`，跨逆变器正负 DC 端 | 逆变器侧 DC 电压候选，不等同于电池端电压 |
| `i_phase` | PMSM/电流检测链和 `blk_182 From_i_abc` | 电机相电流，必须单独定义峰值、平均值和窗口 RMS |
| `I_batt` | `blk_761 Battery_Current_Sensor`，串入 `blk_20` 的 HV 正路径 | 电池支路电流候选；方向和单位仍需模型编译/视觉复核，不得用 `I_dc` 或 `i_phase` 替代 |

`blk_20` 仍保留 `BatteryDC`/`InverterDCFeedback` Selector 信号桥，同时已恢复带电阻、电容和传感器的物理 DC 路径。当前 `DCBus` 取 `BatteryDC[1]`，因此是电池侧电压测量的信号映射；`BatteryLoad` 仍取 `InverterDCFeedback[2]`，且在电池子系统内部被 Terminator 终止，不能自动解释为物理电池端负载电流。

### 4.1 当前电池侧测量方案

此前在 `blk_14 BatteryThermalManagement_BatteryPack` 内直接加入官方 `Current Sensor`、PS-Simulink Converter 和并联电压传感器的尝试已回滚；`blk_14` 保留官方电池物理正负端，当前不在电池包内部直接增加测量分支。

当前采用 `blk_20 DC_Link_And_Inverter_Interface` 内的受控测量方案：

- `blk_761 Battery_Current_Sensor` 串入 `HV_pos -> blk_740 HV_Pos_Path_R -> DC-link 正端` 路径；
- `blk_762 Battery_Voltage_Sensor` 跨接 `HV_POS_BATT_NODE` 与 `HV_NEG_BATT_NODE`；
- `blk_763/764` 为 PS-Simulink Converter，`blk_765/766` 以全局标签输出 `I_batt_meas/V_batt_meas`；
- `blk_769`--`blk_780` 使用四组 `Connection Label` 连接电池正负节点和 DC-link 正负节点，避免直接在 conserving port 上重复分支；
- `blk_14` 的 `Mux_BatteryDC` 已改为 `[V_batt, I_batt, T_pack, SOC]`，对应 `blk_587/588` 已切换到 `V_batt_meas/I_batt_meas`；
- 电池热、RMS、风险和能量审计消费者 `blk_660/667/668/684/686/714` 已切换到电池侧标签。

当前 `blk_14`、`blk_20` 和 root 的 `model_check(all)` 均为 `healthy`，模型已保存且 `Dirty=off`。这证明结构和标签连接已读回，但不等于模型已编译、参数可运行或电池端功率/能量结论已经成立。

### 4.2 DC-link 测点和剩余语义

此前直接对 `blk_20` conserving port 进行多分支重连时曾出现结构回退；改用 `Connection Label` 后，当前四组节点和传感器路径已闭合。读回关系为：

- `I_dc` 仍由 `blk_591 DC_Current_Sensor` 提供，位于逆变器侧 DC 支路；
- `V_dc` 仍由 `blk_592 DC_Voltage_Sensor` 提供，跨逆变器正负 DC 端；
- `I_batt/V_batt` 由 `blk_761/762` 提供，测点位于电池侧 HV 边界；
- `InverterDCFeedback` 仍为 `[V_dc, I_dc, P_dc, status]`，不能与 `BatteryDC` 或 `BatteryLoad` 混用；
- `blk_685 Compute_I_rms_V4G` 仍是 `abs(I_instant)` 代理，`blk_666 Energy_Balance_Audit` 的计算脚本尚未改写。

## 5. 恢复安全记录

本轮发现此前使用含糊范围的清除操作会扩大影响，因此采用以下恢复纪律：

1. 不再使用 `disconnect "? -> ?"` 或未限定 scope 的物理清除。
2. 每次 `model_edit` 只作用于一个明确 scope，失败或 `partial` 后立即 `model_read` 与 `model_check`。
3. 先用实际端口 ID/端口号确认连接，再进行定向断开和重连。
4. 结构恢复后立即保存，并用 root 和关键子系统双重检查。
5. 视觉快照只作为结构审查的第二证据，不把画布连通误判为参数或物理正确。

## 6. 下一步执行顺序

1. 视觉复核 `blk_20` 和 root，确认 `Connection Label` 连接、端口方向、节点没有短接或悬空，并记录 `PortConnectivity` 证据。
2. 单独定义 `BatteryLoad` 的接口语义；不把 `I_batt`、`I_dc` 和 `i_phase` 合并为同一字段。
3. 为 `E_dc_link`、`P_hv_loss`、`Q_hv_gen`、逆变器损耗、PMSM 损耗、热储能和排热建立字段映射；未知项保持 `PLACEHOLDER/UNKNOWN`。
4. 将电池端功率、DC-link 端口功率、逆变器 DC 输入功率、PMSM 电磁/机械功率和热源项放入统一符号约定。
5. 再处理窗口 RMS 和 KPI 记录链；瞬时绝对值只保留为临时诊断量，不能作为权威 RMS。
6. 只有参数身份、适用范围和阻断字段明确后，才决定最小 `Reference / Platform` 工况验证。

## 7. 当前判定

本轮达到：

- `Structural / Interface`：通过，root 和关键 scope 无未连接端口、悬空线或 Stateflow lint 问题。
- `Blocked / Contract`：仍然阻断，HV 路径电阻、DC-link 电容和低温设备边界尚未具备可运行配置身份。
- `Reference / Platform`：尚未运行，不输出数值发热、效率、温升或安全结论。

本文件不改变 M4-00 两份资料文件的历史证据角色，也不把官方示例参数自动写入 M3 默认配置。
