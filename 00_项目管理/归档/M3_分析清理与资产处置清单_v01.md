# M3 分析清理与资产处置清单 v01

日期：2026-07-15
阶段：阶段 1，分析清理（已完成，转入阶段 2）
状态：已共同确认；本文件作为阶段 1 记录保留

## 1. 文档目的

本文件记录 M3 进入后续工程化定义前的现状分析、文本清理结果、资产角色和待处置项。它不是 Simulink 建模计划，不授权模型结构修改、脚本实现、仿真计算或产品结论。

本轮工作严格遵循以下顺序：

```text
分析清理
  -> 工程化系统定义草案
  -> 资产归档收口
  -> 共同对齐的顶层建模规划
```

只有完成前三步并经共同确认后，才冻结后续 V4 迭代范围、优先级、边界和验收条件。

## 2. 当前活动基线

### 2.1 活动 V4 模型

当前唯一活动 Simulink 源模型为：

```text
车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx
```

该文件已从 Git 恢复到活动模型目录，文件大小为 `905646` bytes。MATLAB 只读加载和顶层结构读回已成功。当前读回的主要边界包括：

- `BatteryThermalManagement_BatteryPack`
- `DC_Link_And_Inverter_Interface`
- `MCB_SIUnits_FOC_Controller`
- `PMSMDriveThermal_Inverter_And_Motor`
- `PulseHeating_Command_And_Limits`
- `KPI_And_Logging`
- 电池限制、RMS、损耗和能量审计诊断层

基线指纹记录：最近一次包含该模型的 Git 提交为 `a2cd8dfd9efaa0c6c810e2a20c94dce4b8290c98`（`feat: V4-F battery electrochemical limits (T_min/SOC_limit/Charge/Discharge + lithium risk)`）；当前活动文件 SHA-256 为 `77E6896426BE6543FEFAA8DD37842FD024A109236616CDBB62DCE82F96D65E92`。阶段 1 复核未发现该 `.slx` 有工作区差异。

该模型的正确角色是 **V4 受控工程基线**：

1. 保留其后续工程化改进价值。
2. 作为阶段 2 工程化定义和阶段 4 顶层建模规划的结构证据。
3. 在共同确认顶层规划前不修改其结构、参数、接口或内嵌逻辑。
4. 后续采用直接迭代 V4 的版本策略，不创建并行 M3 模型副本。
5. 每个共同确认的模型里程碑通过后，再由用户决定是否创建 Git 提交。

### 2.2 V4 当前结构状态

本阶段只做结构读回，不将结构状态解释为物理验收。当前 `model_check(all)` 结果为 `warnings`，共 7 条 warning：

- 4 条两端缺失的线路记录。
- `Mux_BatteryStatus` 有未连接目的端记录。
- `Mux_LimitStatus` 有未连接目的端记录。
- `Mux_SystemKPI` 有未连接目的端记录。

这些 warning 应进入后续 V4 问题台账。阶段 4 顶层规划需规定逐项关闭方式；在首个可运行物理工况前，相关 warning 必须关闭，或形成明确、可审计并经确认的保留理由。

当前不得据此声称：

- 能量账本已闭合。
- RMS 指标已具有工程语义。
- 电池、逆变器、PMSM 和控制器已经完成现实设备匹配。
- 低温脉冲加热控制可实现。
- 电池安全、析锂、寿命、退磁、NVH 或量产可行性已验证。

### 2.3 模型缓存

以下内容属于仿真/编译缓存，不是活动模型源文件：

```text
车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slxc
车载脉冲加热技术simlink模型/02_模型/slprj/
车载脉冲加热技术simlink模型/03_脚本/pulse_heating_official_spine_v04.slxc
车载脉冲加热技术simlink模型/03_脚本/slprj/
```

本阶段不清理缓存。缓存不作为结构证据、参数来源或工程结论来源；后续仅在缓存损坏、用户明确要求或专项空间清理时处理。

## 3. 文本入口分析

### 3.1 已发现的主要问题

| 编号 | 问题 | 影响 | 阶段 1 处置 |
|---|---|---|---|
| T-01 | `AGENTS.md`、`README.md` 和目录索引把当前阶段写成独立指标、Precheck 和首个受控组合的实施准备 | 把实现辅助资产置于分析和规划之前 | 改为阶段 1 分析清理；明确当前不实施 |
| T-02 | 活动入口曾写成“没有 active `.slx`”，与已恢复的 V4 活动模型事实冲突 | 可能再次误归档或覆盖 V4 | 改为 V4 位于 `02_模型/` 的活动工程基线 |
| T-03 | M3 v01 将 G0-G3 内容、Precheck 和脚本顺序写成已冻结的实施路线 | 在顶层设计共同确认前提前锁定模型路线 | 保留已有分析内容，标记为待重审规划基线，移除当前固定实施授权 |
| T-04 | 误建的指标/配置/Precheck 脚本被列入 Active 文件 | 会使后续 agent 误认为它们是当前任务主线 | 从 Active 入口摘除，并已于阶段 3 归档至 `归档/M3_阶段2误建资产/` |
| T-05 | `归档/M2_V4-I/` 内历史文档仍保留“当前”“下一步”“active”等原阶段措辞 | 搜索或按关键词读取时可能被误当现行指令 | 保留原文作为证据，由活动入口明确覆盖其指令效力 |
| T-06 | V4 活动模型、损坏归档原件和 Git 恢复副本的角色没有清楚区分 | 可能把恢复证据误当活动模型，或把活动模型错误归档 | 固化三者的不同角色和路径 |

### 3.2 文本权威关系

阶段 1 期间，文本的权威关系如下：

1. `AGENTS.md`：项目规则和当前阶段限制。
2. `README.md`：项目状态总览和读取入口。
3. `00_项目管理/项目目录索引.md`：活动、冻结、历史、缓存和待处置资产索引。
4. 本文件：阶段 1 分析清理和资产处置记录。
5. `M3_工程兼容性模型规划_v01.md`：既有规划基线，当前只作为待重审草案，不作为已确认的最终顶层建模规划。
6. `剥离原则与参数分层规则_v01.md`：参数来源和历史隔离规则。
7. `归档/` 内文件：只读证据和历史记录，不构成现行执行指令。

## 4. 资产分类与当前处置状态

### 4.1 活动资产

| 资产 | 当前角色 | 阶段 1 处置 |
|---|---|---|
| `02_模型/pulse_heating_official_spine_v04.slx` | V4 受控工程基线 | 保留在活动目录，不修改、不归档 |
| `AGENTS.md` | 项目级规则 | 修正阶段、模型和实施限制 |
| `README.md` | 项目状态入口 | 修正阶段和 V4 角色 |
| `00_项目管理/项目目录索引.md` | 资产索引 | 修正活动/归档/待处置分类 |
| `00_项目管理/M3_分析清理与资产处置清单_v01.md` | 阶段 1 记录 | 作为本阶段评审文本 |
| `00_项目管理/M3_工程兼容性模型规划_v01.md` | 既有规划基线 | 标记为待重审，不作为最终规划 |
| `剥离原则与参数分层规则_v01.md` | 参数治理规则 | 暂不改变其参数分层事实 |

### 4.2 冻结和恢复证据

| 资产 | 当前角色 | 阶段 1 处置 |
|---|---|---|
| `归档/M2_V4-I/` 内说明、审计矩阵、参数矩阵 | V4 历史结构、风险和参数冲突证据 | 保留，只读按需取证 |
| `归档/M2_V4-I/02_模型/pulse_heating_official_spine_v04.slx` | 历史归档原件；当前无法加载 | 保留并标记为损坏取证对象，不替代活动模型 |
| `归档/M2_V4-I/02_模型/pulse_heating_official_spine_v04_git_recovery.slx` | Git 恢复证据；可加载 | 保留并标记为恢复证据，不替代活动模型 |
| `归档/M2_V4-I/03_脚本/` | V4 构建和诊断历史 | 保留归档，按需取证，不恢复为活动脚本 |

损坏原件和可加载恢复副本均保留。两者的保留理由不同，均不能被误标为当前活动模型。

### 4.3 历史和参考资产

以下资产继续保留在现有归档目录，不进入默认执行路线：

- `归档/L0.5_4500-27/`
- `归档/v03_诊断沙箱/`
- `归档/历史方法论/`
- `归档/材料池参考/`
- `归档/项目管理快照/`
- `归档/接口契约_v01.md`
- `归档/单电池包-控制器-单电机_规范化建模规划与操作步骤_v02.md`

其中的参数、拓扑、频率、结论和“下一步”只可作为历史证据、反例或候选参考，不自动进入 M3 默认架构、参数或任务顺序。

### 4.4 阶段 3 已归档的误建资产

以下文件由前一轮越界实施产生，当前不纳入 M3 活动路线：

```text
车载脉冲加热技术simlink模型/03_脚本/computeWindowMetrics.m
车载脉冲加热技术simlink模型/03_脚本/createM3ContractConfig.m
车载脉冲加热技术simlink模型/03_脚本/precheckSystemConfig.m
车载脉冲加热技术simlink模型/03_脚本/tests/computeWindowMetricsTest.m
车载脉冲加热技术simlink模型/03_脚本/tests/precheckSystemConfigTest.m
```

当前处置状态：**已从活动入口摘除，并于阶段 3 移入 `归档/M3_阶段2误建资产/`；不作为活动资产使用。**

归档说明见 `归档/M3_阶段2误建资产/README.md`。这些文件即使曾通过静态检查或单元测试，也不构成模型验收、物理正确性、Active 准入或 M3 已确认实施路线的证据。

### 4.5 用户授权的空目录清理

`车载脉冲加热技术simlink模型/outputs/` 已检查为空。该目录没有可追溯结果或其他文件，已按用户明确授权删除。该操作不涉及活动 V4、待归档脚本、`.slxc`、`slprj/` 或其他仿真结果/缓存资产。

## 5. 当前工作限制

阶段 3 资产归档收口已完成；阶段 4 顶层建模规划共同确认前：

1. 不修改 `02_模型/pulse_heating_official_spine_v04.slx` 的结构、参数或内嵌逻辑。
2. 不创建新的 MATLAB/Simulink/Stateflow/Simscape 脚本或模型。
3. 不运行仿真、参数扫描、模型测试或数值计算。
4. 不把 `M3-PD-CONTRACT-v01`、`PLACEHOLDER` 参数或 `V4-REF` 参考配置作为可运行设备系统。
5. 不将 V4 历史脚本批量恢复到 `03_脚本/`。
6. 不清理 `.slxc`、`slprj/` 或其他仿真缓存。
7. 不修改 OpenCode 全局配置、客户端数据库或历史会话记录。

## 6. 已确认的后续规划前提

以下内容已由共同讨论确认，可作为阶段 2 工程化定义草案的输入，但尚未形成最终顶层模型设计：

| 决策项 | 已确认前提 |
|---|---|
| 最小对象 | 单电池包-单电驱模块 |
| V4 版本策略 | 直接迭代活动 V4；不创建并行模型副本；确认里程碑后再决定 Git 提交 |
| 首轮工程问题 | 判断车载自加热路径的工程兼容性可实现性 |
| 首轮场景 | 低温驻车、无牵引需求、受控预热 |
| 路径范围 | 先列候选路径和排除条件，再共同选择；首轮只列车载自加热闭环 |
| 机械边界 | `Park/Lock` 接口优先，名义工况采用理想锁止 |
| 控制责任 | BMS、VCU、MCU/逆变器先表达为约束接口，不伪造真实通信和软件权限 |
| 热目标 | 先定义热源、热路径、热边界和审计口径，不预设产品温升指标 |
| 首个里程碑 | 电流/能量路径与系统能量账本 |
| V4 warning | 先登记后逐项关闭；首个可运行物理工况前全部关闭或形成确认后的保留理由 |
| 误建脚本 | 已于阶段 3 归档，不作为活动资产 |

## 7. 阶段 1 输出与完成记录

阶段 1 的文本输出为：

1. 本文件：分析清理、资产分类、处置状态和当前限制。
2. 修正后的 `AGENTS.md`、`README.md` 和 `项目目录索引.md`：使活动入口与上述事实一致。
3. 修正后的 M3 v01：保留已有规划证据，但取消其当前实施授权和固定实施顺序效力。

阶段 1 已于 2026-07-15 共同确认，确认内容为：

1. V4 活动模型、归档原件和恢复副本的三层角色准确。
2. 误建脚本按“阶段 3 归档”执行。
3. 当前入口不再要求先建脚本、先做 Precheck 或先修改模型。
4. 历史和归档文件已明确降级为只读证据。
5. 空的 `车载脉冲加热技术simlink模型/outputs/` 目录已授权删除，其他缓存和待处置资产保持原位。

阶段 1 已通过，阶段 2 工程化系统定义草案、阶段 3 资产归档收口和阶段 4 顶层建模规划均已共同确认。当前下一步为 M4-00 资料检索与官方案例复用复核。
