# 轻量化交接清单

日期：2026-07-03

清理前快照：

```text
commit 15cd3ab chore: snapshot pulse heating workspace before cleanup
remote origin/main
```

## 保留主线

1. `AGENTS.md`、`README.md` 和 `00_项目管理/`：项目入口、分工、建模路线和阶段边界。
2. `零维脉冲加热仿真模型-4500-27型整车/`：当前 4500-27 L0.5 MATLAB 主线模型、参数表、摘要结果和阶段报告。
3. `零维脉冲加热仿真模型-AMEsim/README.md`：OpenCode 后续 AMESim 骨架入口。
4. `脉冲加热参数整理汇总/`：电池、电驱、多合一和整车参数原始表，暂保留用于建模参数追溯。
5. `建模依据资料/`：论文、专利、技术材料和文献阅读入口。建模依据链默认保留，不作为轻量化优先删除对象。

## 已移出当前工作树

1. `归档资料/`：早期调研、历史脚本、历史模型和旧 PPT。
2. `汇报输出/`：历史汇报、对接表、预读材料和图片。
3. `零维脉冲加热仿真模型-COMSOL/`：COMSOL 原型模型和构建脚本。
4. `零维脉冲加热仿真模型-3806型整车/`：早期 3806 模型。
5. `零维脉冲加热仿真模型-4500-27型整车/05_仿真结果/4500_27_screening/4500_27_scan_results.csv` 和报告图：可再生成结果。
6. 本地 `node_modules` junction 和 `outputs/` 临时目录。

## 恢复方式

恢复单个路径：

```powershell
git restore --source 15cd3ab -- "<相对路径>"
```

查看清理前目录：

```powershell
git ls-tree -r --name-only 15cd3ab
```

原则：文献、专利、参数依据默认保留；轻量化优先处理可再生成结果、历史汇报、旧模型归档、工具缓存和临时输出。

## L0.5 阶段资产归档标记（2026-07-06）

动作：原地标记，不移动文件，不删除文件。

标记对象：`零维脉冲加热仿真模型-4500-27型整车/`（L0.5 零维集总粗筛模型）。

标记位置：该目录 `模型目录说明.md` 顶部。

保留理由：参数脚本、参数表、建模依据清单、逆变器口径、阶段报告和敏感性摘要后续仍有参考价值，属证据链/KPI 口径来源，不清理。

不保留为活跃主线的原因：L0.5 粗筛阶段任务已完成；后续主线调整为 Simulink/Simscape-first（AMESim 备选），L0.5 MATLAB 零维模型不再扩展。

路线文档更新（AGENTS.md / README.md / AMESim README 的 AMESim-first 表述调整为 Simscape-first）留待后续资产整理与建模规划步骤统一处理。
