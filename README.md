# 车载低温脉冲加热系统工程建模

日期：2026-07-17
当前阶段：M4-01 结构恢复、窗口 RMS、未知项感知账本、官方 PWM、FOC 电角坐标和受控开环物理 PWM 路径已完成；`Reference / Platform v02` 参数契约及短时闭环/开环检查已完成，活动资产进入分层能量账本和热路径闭环阶段。用户已授权基于工作原理、官方案例、文献与工程默认值持续完成通用系统集成和 Reference / Platform 参数匹配；活动模型仍不代表具体电池拓扑、设备平台电压或硬件角色。

本仓库用于建立面向工程集成设计的通用低温脉冲加热系统模型。研究对象不是某一具体车型或产品，而是电池、高压配电与 DC-link、逆变器、PMSM、控制器、热管理和保护逻辑之间的多域兼容性。

## 当前目标

M3 的目标是形成通用低温脉冲加热系统集成设计与参数匹配框架，用于回答：

1. 系统凭什么能发热。
2. 产生的热是否合理、能量和热源分配是否可审计。
3. 发热过程如何被控制，控制策略如何受电气、热、机械和保护约束限制。
4. 电压、电流、功率、热、频率、采样、保护与持续时间是否具有共同参数匹配包线。

M3 不直接证明产品安全、寿命、量产可行性或具体设备定型。

## 当前状态

- 阶段 1 分析清理、阶段 2 工程化系统定义草案、阶段 3 资产归档收口和阶段 4 顶层建模规划均已共同确认；M4-00 首轮资料复核完成后，用户已授权进入 M4-01。
- `00_项目管理/M4-00_官方资产复用矩阵_v01.md` 和 `00_项目管理/M4-00_模型影响清单_v01.md` 是 M4-00 的当前交付物和证据输入；用户已授权进入 M4-01，当前按受控最小修改推进。
- `00_项目管理/M4-01_结构恢复与能量账本准备_v01.md` 记录当前模型恢复、视觉审查、官方 PWM/FOC 坐标修正、短时 Reference 控制链检查、参数占位和剩余账本工作。
- `M3_顶层建模规划_v02.md` 是阶段 4 冻结版项目级顶层研究规划；已包含长期能力路线和 M4-00/M4-01/M4-02/M4-03 的近期指导，近期路线不代表整个项目目标只限于四个实施步骤。
- `车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx` 是当前唯一活动的 V4 受控工程基线；本轮保存重载后 root、`blk_7`、`blk_14`、`blk_20`、`blk_27`、`blk_35`、`blk_66`、`blk_86`、`blk_128`、`blk_843` 的结构检查均为 `healthy`，编译无范围或导数警告。这只代表连接结构健康，不代表参数可运行或工程验收通过。
- root 级 `System_Audit` 是无端口审计支撑层，包含 RMS/测量、能量/热和电池包线筛查三个内部子系统；它以既有 `Goto/From` 标签传递审计字段，不增加主物理路径。包线、析锂代理及局部损耗常数仍是待来源契约的候选，不能作为设备、安全或已验证损耗结论。
- `M3-PD-CONTRACT-v01` 是不可运行的契约基线。它用于验证配置、接口与指标，不能因 `PLACEHOLDER` 参数进入 Active 工况。
- `归档/M2_V4-I/` 保存 V4 历史说明、构建/诊断脚本、审计记录、损坏原件和 Git 恢复证据；它们是只读证据，不替代 `02_模型/` 中的活动 V4，也不构成当前执行指令。
- v03 诊断沙箱、L0.5 历史案例、旧计划和旧材料池均已归档；可按需追溯，但不参与默认工作流。
- 我误建的三个 MATLAB 脚本和两份测试已从活动入口摘除，并已归档至 `归档/M3_阶段2误建资产/`；当前不使用。
- `车载脉冲加热技术simlink模型/outputs/` 原为空目录，已按用户授权删除；未清理 `.slxc`、`slprj/` 或其他结果/缓存资产。

## 读取顺序

1. `AGENTS.md`
2. `README.md`
3. `00_项目管理/项目目录索引.md`
4. `00_项目管理/M3_分析清理与资产处置清单_v01.md`
5. `00_项目管理/M3_工程化系统定义草案_v01.md`
6. `00_项目管理/M3_顶层建模规划_v02.md`
7. `00_项目管理/M3_工程兼容性模型规划_v01.md`
8. `00_项目管理/M4-00_官方资产复用矩阵_v01.md`
9. `00_项目管理/M4-00_模型影响清单_v01.md`
10. `00_项目管理/M4-01_结构恢复与能量账本准备_v01.md`
11. `车载脉冲加热技术simlink模型/00_任务计划与资料/剥离原则与参数分层规则_v01.md`

## 核心物理与工程问题

电池加热的一阶近似为：

```text
Q_bat ~= I_rms^2 * R_bat(T, SOC, f)
```

有频域阻抗数据时，应以 `Re(Z_bat(T, SOC, f))` 替代简单电阻近似。工程问题不只是温升，还包括实际电流路径、PWM/采样可实现性、电池充放电与热约束、电驱损耗、保护策略和参数身份一致性。

## 当前阶段门槛

当前阶段 4 顶层建模规划已冻结，M4-00 首轮资料复核已完成，不执行旧 G0-G3 的模型实施路线。既有 G0-G3 内容保留在 `M3_工程兼容性模型规划_v01.md`，作为待重审的规划基线。

项目必须依次完成：

1. 分析清理：明确活动 V4、冻结证据、历史资料、缓存和待处置资产。
2. 工程化定义草案：共同定义问题、边界、候选路径、责任接口、账本和数据缺口。
3. 资产归档收口：按确认结果执行归档和活动入口收口。
4. 共同对齐顶层建模规划：确定 V4 后续迭代的优先级、边界、里程碑和验收。

M4-01 已完成结构恢复、接口审计、BatteryLoad 清理、窗口 RMS、未知项感知能量账本字段骨架、官方 PWM 集成、FOC 电角坐标修正、`Reference / Platform v02` 参数契约和受控开环物理 PWM 路径。闭环与受控开环 `20 ms` 短时 Reference 检查确认三桥臂门极严格互补、模式选择正确、机械转速为零、DC-link 能量差与状态一致、RMS 窗口完成，且 `P_dc_link_storage` 显式保持 `UNKNOWN/NaN`。用户已授权继续建立通用 `Reference / Platform` 能量/热账本工况并补齐分层有效标志；不把结构 `healthy`、视觉连通或后续平台仿真升级为具体设备、热性能、安全或产品级结论。

## 资产角色

| 角色 | 位置 |
|---|---|
| Active engineering baseline | `车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx` |
| Active planning and cleanup | `00_项目管理/` 中的阶段记录、目录索引、阶段 4 顶层规划冻结版和待重审 M3 规划基线；根 `AGENTS.md`、本文件；参数分层规则 |
| Frozen M2 evidence | `车载脉冲加热技术simlink模型/00_任务计划与资料/归档/M2_V4-I/`，不替代活动 V4 |
| 历史和参考资料 | `车载脉冲加热技术simlink模型/00_任务计划与资料/归档/` |
| M3 future model/scripts/results | 本轮仅在活动 V4 内完成了已记录的短时闭环 Reference 控制链检查，未新建脚本或结果资产；后续模型、脚本或完整工况仍须在明确授权后受控进入 `02_模型/`、`03_脚本/`、`04_仿真结果/` |

## 结论边界

所有输出必须区分已验证事实、基于证据的推断和待确认假设。模型完成运行、接口输出有限值或脚本判据通过，不等于物理正确、设备匹配、析锂安全、寿命满足或工程放行。

用户已明确持续授权：在整个建模期间无需提供具体参数，模型可基于工作原理、官方案例、文献和工程默认值形成通用架构集成与 Reference / Platform 参数匹配设计。阶段 4 顶层规划冻结版见 `M3_顶层建模规划_v02.md`；M4-00 两份资料交付物作为证据保留，当前结构恢复和剩余缺口见 `M4-01_结构恢复与能量账本准备_v01.md`。误建脚本和测试已按阶段 3 计划归档，空的 `outputs/` 目录已按用户明确授权删除；缓存保持原位。

## 当前接续

本轮完成 M4-01 结构恢复与 Reference / Platform v02 参数契约：`HVPath_R=0.01 Ohm`、`DCLink_C=470e-6 F`、`DCLink_V0=0 V`、`RMS_Window_s=0.01 s`、`IGBT_Ron_ref=1e-3 Ohm`、`IGBT_Vf_ref=1.2 V`，身份为 `OFFICIAL+ENGINEERING_DEFAULT`。在 `blk_20 DC_Link_And_Inverter_Interface` 内通过 `Connection Label` 建立电池侧 `I_batt/V_batt` 测量骨架；退役的 `BatteryLoad` 输入、输出和 root 终端已删除；`BatteryDC=[V_batt,I_batt,T_pack,SOC]` 为发布向量，逆变器侧 `V_dc/I_dc/P_dc` 保持独立。`Compute_I_rms_V4G` 以实际时钟实现 `I_batt`、`I_dc`、`I_phase` 和 `I_heat_equiv` 的窗口 RMS；`Energy_Balance_Audit` 扩展为未知项感知账本，`SystemKPI` 扩展到 44 字段。`blk_7` 采用官方三相两电平 PWM，将机械角转换为零偏置、电角 per-unit 的 Q-axis FOC 输入；开环 duty 经去零序和 `vdc` 缩放后由 `Vabc_Physical_Select` 送入同一官方 PWM。闭环 `20 ms` 检查确认三桥臂门极严格互补、`mean(i_d)=-39.98937 A`、`mean(i_q)=-0.01811 A`、机械转速为零；受控开环 `20 ms` 检查确认实际模式为 `0`、`PWM_Vabc_Command=OpenLoop_Vabc`、门极互补、转速为零。`E_dc_link` 与 `DeltaE_dc_link` 在短时工况末端一致（`1.09499578 J`），`P_dc_link_storage` 显式保持 `UNKNOWN/NaN`，`full_ledger_valid=0`。保存重载后 root、`blk_7`、`blk_14`、`blk_20`、`blk_27`、`blk_35`、`blk_128`、`blk_843` 均为 `healthy`，编译无范围或导数警告。仍未验证死区、完整热路径、电池储能、热储能、排热、设备能力或安全；`DeltaE_batt`、热源去向和低温保护数据仍阻断工程结论。
