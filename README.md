# 车载低温脉冲加热系统工程建模

日期：2026-07-22
当前阶段：**M4 已正式收尾并冻结为参考基线，M4-M5 过渡收尾已完成；M5-01 已准入但尚未启动。** M4-00~M4-03B 全部完成，收口判据全部满足。2026-07-22 完成：历史脚本归档、过期缓存清理、建模仿真研究规范建立（`AGENTS.md` 第 11 节）、默认参数总表、技术原理研究报告、8 张 MATLAB 可视化图表，以及 7 篇重点原件的逐页视觉核验。M4 正式命名为 CMI（Combined Module Invert，文献原称）架构下的开环低频 d 轴脉冲电流自加热路线；M5 首版正式命名为 dSVPWM（dead-zone Space Vector PWM，死区空间矢量脉宽调制）换流型脉冲自加热路线。M5-00 已完成文献分级、分层路线选择、过渡审计和 v2 接口契约；首版保持无新增硬件边界，频域电热和析锂判据分别作为模型层和安全层。v2 唯一模型尚未建立，且在用户明确启动 M5-01 前不创建模型、脚本或新结果；任何活动模型仍不代表具体电池拓扑、设备平台电压或硬件角色。

本仓库用于建立面向工程集成设计的通用低温脉冲加热系统模型。研究对象不是某一具体车型或产品，而是电池、高压配电与 DC-link、逆变器、PMSM、控制器、热管理和保护逻辑之间的多域兼容性。

## 当前目标

M3 的目标是形成通用低温脉冲加热系统集成设计与参数匹配框架，用于回答：

1. 系统凭什么能发热。
2. 产生的热是否合理、能量和热源分配是否可审计。
3. 发热过程如何被控制，控制策略如何受电气、热、机械和保护约束限制。
4. 电压、电流、功率、热、频率、采样、保护与持续时间是否具有共同参数匹配包线。

M3 不直接证明产品安全、寿命、量产可行性或具体设备定型。

## 当前状态

- 阶段 1 分析清理、阶段 2 工程化系统定义草案、阶段 3 资产归档收口和阶段 4 顶层建模规划均已共同确认；**M4 已正式收尾**（见 `00_项目管理/M4_阶段收尾报告_v01.md`），`车载脉冲加热技术simlink模型/` 冻结为参考基线。M4-M5 过渡收尾已完成，M5-01 仅保留准入条件，尚未启动。M4 收口历史判据见 `00_项目管理/M4_阶段目标达成审计与收口分解_v01.md`。
- M4-00、M4-01A、M4-01B、M4-01C、M4-02A、M4-02B、M4-03A 和 M4-03B 已形成证据。M4 收口判据全部满足。
- `00_项目管理/M4-00_官方资产复用矩阵_v01.md`、`00_项目管理/M4-00_模型影响清单_v01.md`、`00_项目管理/M4-01_结构恢复与能量账本准备_v01.md`、`00_项目管理/M4-02_参数敏感性分析计划_v01.md` 和 `00_项目管理/M4-03_控制策略比较计划_v01.md` 是已有阶段证据与执行记录。
- M4-02 使用 `SimulationInput.setBlockParameter` 完成 11 组 40 ms 窗口扫描，未改写活动 `.slx` 默认参数；`DCLink_C` 和 `Rs` 已确认影响物理路径，`R_pack` 与 IGBT 参数当前仅作用于审计代理。
- M4-03 以同一物理基线完成 7 组闭环脉冲比较，验证频率、占空比和幅值覆盖均到达实际 `Id_Ref`；频率存在跟踪与电池 RMS 电流权衡，占空比形成方向偏置，幅值提高增加当前模型中的纹波、铜耗和跟踪误差。
- `M3_顶层建模规划_v02.md` 是阶段 4 冻结版项目级顶层研究规划；已包含长期能力路线和 M4-00/M4-01/M4-02/M4-03 的近期指导，近期路线不代表整个项目目标只限于四个实施步骤。
- `车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx` 是 M4 冻结的 V4 受控工程基线；M4-01C 保存、关闭和重载后 root 与 `blk_843` 的结构检查为 `healthy`，模型可编译。这只代表连接结构健康和受控 Reference 可运行，不代表工程验收通过。
- root 级 `System_Audit` 是无端口审计支撑层，包含 RMS/测量、能量/热和电池包线筛查三个内部子系统；它以既有 `Goto/From` 标签传递审计字段，不增加主物理路径。包线、析锂代理及局部损耗常数仍是待来源契约的候选，不能作为设备、安全或已验证损耗结论。
- `M3-PD-CONTRACT-v01` 是不可运行的契约基线。它用于验证配置、接口与指标，不能因 `PLACEHOLDER` 参数进入 Active 工况。
- `归档/M2_V4-I/` 保存 V4 历史说明、构建/诊断脚本、审计记录、损坏原件和 Git 恢复证据；它们是只读证据，不替代 `02_模型/` 中的活动 V4，也不构成当前执行指令。
- v03 诊断沙箱、L0.5 历史案例、旧计划和旧材料池均已归档；可按需追溯，但不参与默认工作流。
- 我误建的三个 MATLAB 脚本和两份测试已从活动入口摘除，并已归档至 `归档/M3_阶段2误建资产/`；当前不使用。
- `车载脉冲加热技术simlink模型/outputs/` 原为空目录，已按用户授权删除；未清理 `.slxc`、`slprj/` 或其他结果/缓存资产。

## 读取顺序

1. `AGENTS.md`
2. `README.md`
3. `00_项目管理/项目目录索引.md`
4. `00_项目管理/M3_分析清理与资产处置清单_v01.md`
5. `00_项目管理/M3_工程化系统定义草案_v01.md`
6. `00_项目管理/M3_顶层建模规划_v02.md`
7. `00_项目管理/M3_工程兼容性模型规划_v01.md`
8. `00_项目管理/M4-00_官方资产复用矩阵_v01.md`
9. `00_项目管理/M4-00_模型影响清单_v01.md`
10. `00_项目管理/M4-01_结构恢复与能量账本准备_v01.md`
11. `00_项目管理/M4_阶段目标达成审计与收口分解_v01.md`
12. `00_项目管理/M4-02_参数敏感性分析计划_v01.md`
13. `00_项目管理/M4-03_控制策略比较计划_v01.md`
14. `车载脉冲加热技术simlink模型/00_任务计划与资料/剥离原则与参数分层规则_v01.md`
15. `00_项目管理/M4_阶段收尾报告_v01.md`
16. `00_项目管理/M5_文献调研与新平台建设_任务规划_v01.md`
17. `00_项目管理/M5_文献评估报告_v01.md`
18. `00_项目管理/M5_文献调研与差异分析_v01.md`
19. `00_项目管理/M4-M5_过渡收尾与M5准入审计_v01.md`

M5 阶段接手另读 `01_低温脉冲加热技术参考资料/README.md` 与 `车载脉冲加热仿真平台_v2/00_任务计划与资料/M5_v2_准入与接口契约_v01.md`。当前文献分级、正式路线命名和首版规划路线以 `00_项目管理/M5_文献评估报告_v01.md`、`M5_文献调研与差异分析_v01.md` 和 `M5_建模参考取舍清单_v01.md` 为准；CN679、CN173、ATIC 的原件核验仍是参数证据补强项。

## 核心物理与工程问题

电池加热的一阶近似为：

```text
Q_bat ~= I_rms^2 * R_bat(T, SOC, f)
```

有频域阻抗数据时，应以 `Re(Z_bat(T, SOC, f))` 替代简单电阻近似。工程问题不只是温升，还包括实际电流路径、PWM/采样可实现性、电池充放电与热约束、电驱损耗、保护策略和参数身份一致性。

## M4-M5 过渡收尾状态

`M4-M5_过渡收尾与M5准入审计_v01.md` 已确认 M4 收口、模型冻结、历史脚本归档、结果保留、核心文献证据和 v2 接口契约均已完成。M5-01 已具备准入条件，但没有启动任务。

当前维持以下收尾状态：

1. 保留 `车载脉冲加热仿真平台_v2/` 的目录骨架和 `M5_v2_准入与接口契约_v01.md`，不创建 `.slx`。
2. 保留 dSVPWM、频域电热和安全候选的信号/参数边界，等待后续任务确认。
3. 仅在用户明确启动 M5-01 后，才进行结构检查、编译、实际门极读回、独立 RMS 和固定窗口账本验证。

当前禁止以下行为：

1. 在 M4 冻结 V4 上继续开发，或将 M4 CMI 架构下的开环低频 `Id` 脉冲复制为 v2 首版控制结论。
2. 将文献数值、图估 EIS 参数、M4 平台参数或 `PLACEHOLDER` 升级为设备默认值。
3. 因模型能够运行而将 `DeviceProtection_Evaluable` 置为 `1`，或宣称析锂安全、寿命和产品能力。

## 资产角色

| 角色 | 位置 |
|---|---|
| M4 frozen reference baseline | `车载脉冲加热技术simlink模型/02_模型/pulse_heating_official_spine_v04.slx` |
| M5 active planning and contract | `00_项目管理/` 中的 M5 规划、评估、过渡审计；根 `AGENTS.md`、本文件；`车载脉冲加热仿真平台_v2/00_任务计划与资料/` 下的 v2 准入契约 |
| 历史脚本归档 | `车载脉冲加热技术simlink模型/00_任务计划与资料/归档/M4_历史脚本/`，含 M4-02A/M4-03A 运行器 |
| Frozen M2 evidence | `车载脉冲加热技术simlink模型/00_任务计划与资料/归档/M2_V4-I/`，不替代活动 V4 |
| 历史和参考资料 | `车载脉冲加热技术simlink模型/00_任务计划与资料/归档/` |
| M4 冻结脚本/结果 | `03_脚本/runM401CThermalLedger.m`、`runM402BCompleteLedgerSensitivity.m`、`runM403BControlComparison.m`、`03_脚本/plot/plotAll.m`；`04_仿真结果/` 下对应 CSV |
| M4 冻结参考基线 | `车载脉冲加热技术simlink模型/`（2026-07-22 冻结，只读参考，不在其中做新开发） |
| v2 平台（M5-01，未启动） | `车载脉冲加热仿真平台_v2/`（文献验证型，接口契约已建立，唯一 `.slx` 尚未创建） |
| 文献库 | `01_低温脉冲加热技术参考资料/`（26 篇 PDF + 壳牌方案 PPT 截图，任务 2 调研对象） |

## 结论边界

所有输出必须区分已验证事实、基于证据的推断和待确认假设。模型完成运行、接口输出有限值或脚本判据通过，不等于物理正确、设备匹配、析锂安全、寿命满足或工程放行。

用户已明确持续授权：在整个建模期间无需提供具体参数，模型可基于工作原理、官方案例、文献和工程默认值形成通用架构集成与 Reference / Platform 参数匹配设计。阶段 4 顶层规划冻结版见 `M3_顶层建模规划_v02.md`；M4-00 两份资料交付物、M4-01 结构恢复记录、M4-02 扫描记录和 M4-03 控制比较记录均作为证据保留。误建脚本和测试已按阶段 3 计划归档，空的 `outputs/` 目录已按用户明确授权删除；缓存保持原位。

## 当前接续

**M4-M5 过渡收尾已完成，当前不启动 M5-01**：v2 仅保留目录骨架和接口契约，不建立模型、不迁移接口、不运行新工况。M5 规划见 `00_项目管理/M5_文献调研与新平台建设_任务规划_v01.md`，准入条件见 `M4-M5_过渡收尾与M5准入审计_v01.md`。M4 的设备能力、低温安全、析锂、寿命和产品热边界仍保持 `UNKNOWN/NaN` 或不作结论。

以下为 M4 收尾前的执行记录（保留作为历史证据）：

M4-01B 基础保持不变，并由 M4-01C 新增 `blk_896/blk_897` 的电池温度发布和固定窗口热账本，以及 `blk_647/blk_639/blk_666` 的 Reference 损耗、锁止铁耗和系统残差语义。`SystemKPI` 保持 45 字段；`u44=Energy_Ledger_Valid`、`u45=Electrical_Ledger_Valid`。完成窗口内 `DeltaE_batt=-1.28540044107756 J`、`P_thermal_storage=-111.739818340343 W`、`P_heat_rejection=211.755632881919 W`、`P_unmodeled=46.7653709933744 W`；这些是短时锁止 Reference 字段，不验证设备热路径或安全。

M4-02 随后以临时块参数覆盖完成 11 组 `0.08 s` 电气先行扫描，统一使用 `[0.04,0.08] s` 的 40 ms 口径。名义工况的 `V_dc` 纹波为 `10.802 V`、`I_batt` RMS 为 `5.440 A`、平均 `P_batt` 为 `100.956 W`；电容减半/加倍对应纹波 `17.936 / 5.809 V`，`Rs` 半值/双值对应 `P_cu` `16.014 / 63.589 W`。`R_pack` 和 IGBT 参数只改变热源/导通损耗代理，未改变物理电压、电流或控制响应。全部工况门极重叠/互补失配为零、闭环有效、速度为零、完整账本保持无效，扫描后 root `healthy`、模型 `Dirty=off`。这些是平台局部趋势，不是设备、热性能或安全结论；详细审计见 M4-02 记录和唯一 CSV。

M4-03 随后以同一物理名义值完成 7 组闭环双极 `Id` 脉冲比较。频率 `25/50/100 Hz` 在 40 ms 中分别形成 `2/4/8` 次参考跃迁；25 Hz 的 `Id` 跟踪 RMS 误差为 `11.525 A`，100 Hz 为 `17.940 A`，但较低频率的电池 RMS 电流更高。20/40/60 A 幅值对应 `P_cu` `9.515 / 31.894 / 68.546 W`、DC-link 纹波 `3.477 / 10.802 / 17.968 V`，并且 60 A 的跟踪 RMS 误差为 `23.815 A`。25%/75% 占空比形成 `-20/+20 A` 的实际平均参考，不与对称脉冲混合排序。全部工况门极严格互补、闭环有效、速度为零、账本保持无效，扫描后 root `healthy`、模型 `Dirty=off`。这些是平台控制趋势与数据缺口，不是频率、幅值、设备能力或安全建议；详细审计见 M4-03 记录和唯一 CSV。
